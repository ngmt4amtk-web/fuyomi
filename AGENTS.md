# ふよみ — 作業契約

ホーム `~/AGENTS.md` を継承。着手前に必ず `docs/decisions.md`（最優先）、`SPEC.md`（制約台帳）、
`CONTRACT.md`（API契約）を読む。

- 割り当てられたファイルだけを書く。他は読むだけ。git コマンドは実行しない。
- 外部ライブラリ・CDN・フォント・ビルド工程を追加しない。ネットなしで動くこと。
- 契約の関数名・引数・戻り値を勝手に変えない。変える必要があると判断したら実装せず報告する。
- 推測で埋めない。分からないことはコメントで「未確認」と書き残す。
- 検証していないことを「できた」と書かない。
- 日本語のコメントで「なぜそうしたか」を書く。何をしているかはコードが言う。

## この場所について
このリポジトリは `~/fuyomi`（ローカル）にある。iCloud 配下ではない。
iCloud 上で作業したところ macOS の権限で丸ごとアクセス不能になったため、2026-08-23 に移した。
