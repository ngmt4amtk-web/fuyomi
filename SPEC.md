# ふよみ — 制約台帳（着手前に必ず読む・作業中も区切りごとに再読）

大人の初心者バイオリン向け「譜読み → 弾く」アプリ。姉妹アプリ「ゆびいろ」
(https://ngmt4amtk-web.github.io/yubi-iro/) の卒業先。
`docs/decisions.md` が最優先の決定事項。この台帳と食い違ったら decisions.md が正。

## 完了の定義（機械判定可能）
1. `python3 -m http.server` で開いた `index.html` で、ト音記号・調号つきの五線譜に
   4分音符4つのフレーズが表示される。
2. マイクで1音ずつ左から判定され、合格した音符が視覚的に変わる。
3. 4音を弾き終えると次のフレーズが出る。設定した問数で結果画面へ。
4. `node tests/run.mjs` が exit 0。
5. GitHub Pages で https://ngmt4amtk-web.github.io/fuyomi/ が開ける。

## 何を作るのか（ゆびいろとの違い＝存在理由）
| | ゆびいろ | ふよみ（これ） |
|---|---|---|
| 対象 | 子ども・初心者 | 大人の初心者 |
| 出題 | 弦の色 + 指番号 | 五線譜（ト音記号・調号） |
| 単位 | 1音 | 4音のフレーズ |
| 演出 | うさぎ・紙吹雪 | 静かな色変化と短い和音 |
| 鍛える力 | 弦と指を体に入れる | 音符 → 弦と指 の変換（＝譜読み） |

## 禁止事項（破ると作り直し）
- 演奏中に音程の針・セント表示を出さない。針を出すと「針を追う練習」になる。
  画面に出るのは伸ばせている時間のバーだけ。当たり外れは音が決まってから出す。
- 音程の点数（100点）を出さない。譜読みの成否と音程の精度を混ぜない。
- 外しても先に進めない。同じ音を続ける。
- **「いまのは ラ線の1に聞こえたよ」と言わない。** マイクは弦も指も観測していない。
  正しくは「いまのは ド♯ の高さに聞こえたよ（ラ線の1の高さ）」。音名が主語。
- 答え（音名・弦・指）を既定で常時表示しない。読まずに正解できてしまう。
- 子ども向けの演出（うさぎ・紙吹雪・大量の効果音）を持ち込まない。
- 外部ライブラリ・CDN・フォント読み込み・ビルド工程を一切使わない。ネットなしで動くこと。
  ト音記号も♯記号も自前のSVGパスで描く。Unicode音楽記号（𝄞 U+1D11E）に頼らない。
- ピッチ検出エンジンを書き直さない。ゆびいろの実装を移植したものが `js/pitch.js` にある。
- 演奏中に押さないと進まないボタンを作らない。バイオリンを構えると両手が塞がる。
- 破壊的な git 操作（reset --hard / force push / ブランチ削除）はしない。
- 各エージェントは自分に割り当てられたファイルだけを書く。他は読むだけ。git は触らない。

## 引き継ぐ実装知（ゆびいろの実機検証の結果。変えると必ず退化する）
- マイクは `echoCancellation` / `autoGainControl` / `noiseSuppression` をすべて false。
  iOS Safari は既定で全部 ON で、これがバイオリンの持続音を削って音程を飛ばす。
- 合格判定は「窓の中の最古サンプルまでの時間」で測ってはいけない。窓は常に1フレーム分だけ
  内側にあるので、その測り方だと時間が hold に構造的に届かず、合格できるかが端末の
  フレームレート次第になる。**held = now - hold[0].t（途切れずに鳴らせている合計時間）で測る。**
- ゆれ幅は最大値ではなく85パーセンタイルで見る。初心者の音は倍音を1〜2フレームだけ拾うので、
  max だと外れ値ひとつ（＝1200セント）で窓ごと落ちて永久に合格できない。
- 規定の2倍の長さまで鳴らしても通らないときは黙って止まらずに判定へ回す。
  無反応で止まるのがいちばん心を折る。
- 出題集合と判定候補は分ける。出題はレベルの範囲内だが、判定候補には現在の調で
  第1ポジションから出せる4弦すべての音を渡す。同じ高さは指の少ない運指を1つだけ残す。
- 判定は3段の救済つき。①目標から生の距離が±tol以内なら無条件で正解
  ②生の距離が±250セント以内で、4弦の候補中の最近傍が目標なら正解
  ③目標までの距離をオクターブで畳み、その距離が±tol以内なら倍音として正解。
  ±250セントは第2段だけの音域外安全弁であり、第3段の入口には使わない。
- 合格・不合格・とばした後の `reset()` は保持を捨てるだけでなく、無声を1フレーム観測するまで
  次の有声を数えない。通常の持続中に混じる短い検出抜けは従来どおり220msまで許容する。
- 同じ高さが2箇所にある場合（レ線の4 ＝ ラ線の0）は音では区別できない。判定は通し、
  画面に「※ ラ線の0と同じ高さ」と出す。
- 音を鳴らしている間は `muteUntil` でマイク判定と「最初の発音」記録を塞ぐ。塞がないと
  自分の出した音を本人の発音として記録したり、次の音をいきなり誤判定したりする。
- `getUserMedia` 成功後のマイク初期化に失敗したら、取得済みトラックと作成済み
  `AudioContext` を解放してから失敗を返す。別画面へ移った後の遅い成否は現在の練習へ反映しない。
- hold は 225 / 300 / 400ms の3段。450/600/800 は長すぎて伸ばしきれなかった。

## 大人向けであることの中身（UIの判断基準）
- ひらがなだらけにしない。漢字を普通に使う。専門語には初出で一言の説明を添える。
- 「できていない」を責めない。無反応で止めない。何が起きたか必ず言葉で返す。
- 色は落ち着いたトーン。合格は音符が濃くなる程度。
- iPhone Safari 第一。タップ対象44px以上。`prefers-color-scheme` でダーク対応。
- 譜面が主役。ボタンとバーは脇役。画面を情報で埋めない。

## 設計の決めごと
- 拍・テンポは判定しない。全部4分音符で表示する。「初見演奏の練習」とは名乗らない。
  名乗るのは「譜読みの土台」まで。
- 調は C / G / D / A の4つ。既定は A（イ長調）。運指は第1ポジション固定。
- 開放弦は「音階の音」ではなく「弦の名前」。調に関係なく出る。ただし譜面では臨時記号で
  正しく書く（イ長調の調号は G♯ を指すので、開放ソ線には ♮ を付ける）。
- フレーズは乱数の羅列にしない。順次進行を主体に、**跳躍は最大4度・1フレーズに1回まで**、
  終わりは安定音（主音・第3音・第5音）。同音の連続は2回まで。
- 結果は「レベル3合格」と言わない。「今回出た音」と言う。出題は範囲を履修していない。
- やり直しは音高を主キーに集計する。同じ音高を別の運指で勧めた記録も1件にまとめ、
  文言は「ド♯で2回やり直した（勧めた運指: ラ線の2）」のように音名を主語にする。
